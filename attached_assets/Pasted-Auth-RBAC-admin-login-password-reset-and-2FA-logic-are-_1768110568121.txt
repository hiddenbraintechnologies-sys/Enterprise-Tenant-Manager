Auth, RBAC, admin login, password reset, and 2FA logic are FROZEN.
Do NOT modify any existing auth behavior, middleware, guards, or endpoints.

Goal:
Add self-service admin security UI using EXISTING APIs only.

================================================
1) Create Security Settings Page (Admins)
================================================
Route:
- /admin/security

Access:
- PLATFORM_SUPER_ADMIN
- PLATFORM_ADMIN
- TECH_SUPPORT_MANAGER
(Block all tenant users)

------------------------------------------------
UI Sections
------------------------------------------------

A) Two-Factor Authentication (TOTP)
- Show current status:
  Enabled / Disabled
- If Disabled:
  - Button: "Enable 2FA"
  - Launch existing 2FA setup flow (QR + code verify)
- If Enabled:
  - Button: "Disable 2FA"
  - Confirmation modal required
- Admins can manage ONLY their own 2FA here

Rules:
- Do NOT allow admins to manage other users’ 2FA
- SUPER_ADMIN 2FA remains required (do not bypass)

------------------------------------------------

B) Backup Codes
- Button: "Regenerate Backup Codes"
- Warn: "This will invalidate existing codes"
- Show new codes ONCE only
- Use existing backend logic

------------------------------------------------

C) Recent Security Activity (Read-only)
Show last 10 entries:
- Login success
- OTP challenge
- Password reset
- Session revoked

Data source:
- Existing audit logs
(No new logging)

================================================
2) Create Session Management Page
================================================
Route:
- /admin/security/sessions

------------------------------------------------
UI Table
------------------------------------------------
Columns:
- Device / Browser
- IP Address
- Country
- Created At
- Last Active
- Current Session (badge)

------------------------------------------------
Actions
------------------------------------------------
- "Revoke this session"
- "Revoke all other sessions"

Rules:
- Admins can revoke ONLY their own sessions
- Current session cannot revoke itself (disable button)

Backend:
- Use existing session / refresh-token storage
- Do NOT create new auth endpoints

================================================
3) Navigation
================================================
Add to Admin Sidebar:
- "Security" → /admin/security
- "Sessions" → /admin/security/sessions

Visibility:
- Admin roles only
- Hidden from tenant users

================================================
4) Guardrails (Important)
================================================
- No changes to:
  /api/auth/*
  RBAC guards
  Login flows
  Password reset
- UI must rely on existing endpoints
- Any forbidden access → show “Not Authorized”

================================================
5) Tests (Minimal, Required)
================================================
Add UI/integration tests:
- Admin can open /admin/security
- Tenant user gets blocked
- Enable/disable own 2FA works
- Sessions list loads
- Session revoke works
- All existing tests must still pass

================================================
6) Deliverables
================================================
- Security Settings page
- Session Management page
- Navigation update
- Test summary (no regressions)

Final Output:
- List of files changed
- Confirmation: "Auth frozen respected"
- Test results
